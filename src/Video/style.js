const video = {
  position: 'absolute',
  top: 0,
  left: 0,
  bottom: 0,
  right: 0,
  width: '100%',
  height: '100%',
};

const container = {
  position: 'relative',
  overflow: 'hidden',
  width: '100%',
  pt: '56.25%',
};

export default { video, container };
